# Backend API base URL (no trailing slash).
# Local dev defaults to http://localhost:8000 if this is unset.
# In production (Vercel), set this to your deployed backend, e.g.
#   VITE_API_URL=https://newsletter-backend.onrender.com
VITE_API_URL=
